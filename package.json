{
  "name": "Text Emoji Generator",
  "version": "1.5.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . \"Text Emoji Generator\" --platform=darwin --arch=arm64 --electronVersion=19.0.0 --icon=logo.icns --out=$PACKAGE_OUT_PATH --overwrite --asar"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "electron": "^23.0.0",
    "electron-packager": "^17.1.1"
  }
}
